extends layout 

block content

    h1= title 
    p Do you really want to delete this #{type}

    form(action="", method="post")
        button(type="Submit", value=dev._id) Yes 
    a(href=dev.url) 
        button(type=button) No