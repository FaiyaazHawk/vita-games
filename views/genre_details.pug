extends layout 

block content 
    h1 Genre: #{genre.name}

    div 

        h4 Games 

        dl 
            each game in genre_games
             dt 
              a(href=game.url)  #{game.title}
             dd #{game.description}

            else 
             p This genre has no games