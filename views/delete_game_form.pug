extends layout 

block content

    h1= title 
    p Do you really want to delete this #{type}

    form(action="", method="post")
        button(type="Submit", value=game._id) Yes 
    a(href=game.url) 
        button(type=button) No