extends layout 

block content

    h1= title 
    p Do you really want to delete this #{type}

    form(action="", method="post")
        button(type="Submit", value=genre._id) Yes 
    a(href=genre.url) 
        button(type=button) No